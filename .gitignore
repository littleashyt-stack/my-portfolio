node_modules
.next

# Secrets — never commit these
.env
.env.local
.env.*.local

# App data: contains bcrypt-hashed admin credentials + all site content
data/

# Uploaded media
public/uploads/*
!public/uploads/.gitkeep
